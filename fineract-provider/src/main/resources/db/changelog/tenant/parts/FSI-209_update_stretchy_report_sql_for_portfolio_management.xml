<?xml version="1.0" encoding="UTF-8"?>
<!--

    Licensed to the Apache Software Foundation (ASF) under one
    or more contributor license agreements. See the NOTICE file
    distributed with this work for additional information
    regarding copyright ownership. The ASF licenses this file
    to you under the Apache License, Version 2.0 (the
    "License"); you may not use this file except in compliance
    with the License. You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing,
    software distributed under the License is distributed on an
    "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, either express or implied. See the License for the
    specific language governing permissions and limitations
    under the License.

-->
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">
    <changeSet author="deepika@fiter.io" id="add_loan_status_filter_in_portfolio_management_report">
        <update tableName="stretchy_report">
            <column name="report_sql"><![CDATA[select
                                                 DISTINCT l.id,
                                                 l.account_no as 'Loan Number',
                                                 st.display_name as 'Loan Officer',
                                                 case
                                                  when c.legal_form_enum = 1 then concat(c.firstname, ' ', c.lastname)
                                                  when c.legal_form_enum = 2 then c.display_name
                                                  end                                                       as 'Client Name',
                                                 c.external_id as 'Client UUID',
                                                  case
                                                    when c.legal_form_enum = 1 then 'Individual'
                                                    when c.legal_form_enum = 2 then 'Entity'
                                                    end                                                         as 'Client Type',
                                                 p.name as 'Loan Product',
                                                 loanPurposeTble.code_value as 'Purpose',
                                                 cvd.code_value as 'Department',
                                                 cvs.code_value as 'Strata',
                                                 l.kiva_id as 'KIVA Loan ID',
                                                 f.name as 'Funder',
                                                 case
                                                     c.legal_form_enum when 1 then coalesce(coi.national_identification_number, 'NA')
                                                     else coalesce(cid.incorp_no, 'NA')
                                                 end as 'Client ID',
                                                 c.date_of_birth as 'Date of Birth',
                                                 c.kiva_id as 'KIVA Client ID',
                                                 loancountTbl.incremental_count as 'Cycle',
                                                 cvc.code_value as 'Cohort',
                                                 case
 when c.legal_form_enum = 1 then cvg.code_value
 when c.legal_form_enum = 2 then cvbog.code_value
 end as 'Gender',
                                                 cvp.code_value as 'Province',
                                                 adr.physical_address_sector as 'Sector',
                                                 adr.physical_address_cell as 'Cell' ,
                                                 adr.physical_address_district as 'District',
                                                 case
 when c.legal_form_enum = 1 then cvn.code_value
 when c.legal_form_enum = 2 then cvbon.code_value
 end as 'Nationality',
                                                 coi.telephone_no as 'Telephone',
                                                 cai.alt_phone_no as 'Mobile No',
                                                 l.submittedon_date as 'Submission Date ',
                                                 l.approvedon_date as 'Approval Date',
                                                 l.disbursedon_date as 'Disbursement Date',
                                                 l.principal_amount_proposed as 'Applied Amount',
                                                 l.approved_principal as 'Approved Amount',
                                                 l.principal_disbursed_derived as 'Disbursed Amount',
                                                 (l.approved_principal - l.principal_disbursed_derived) as 'Difference',
                                                 currency.name as 'Currency Type',
                                                 case
                                                     l.repayment_period_frequency_enum when 0 then 'Daily'
                                                     when 1 then 'Weelky'
                                                     when 2 then 'Monthly'
                                                     when 3 then 'Yearly'
                                                 end as 'Re-payment Term',
                                                 case
                                                     when l.loan_type_enum = 1 then 'Individual'
                                                     when l.loan_type_enum = 2 then 'Group'
                                                     when l.loan_type_enum = 3 then 'JLG'
                                                     when l.loan_type_enum = 4 then 'GLIM'
                                                     when l.loan_type_enum = 5 then 'GSIM'
                                                 end as 'Loan Type',
                                                 l.term_frequency as 'Terms Duration',
                                                 ifnull(paymentTbl1.Actual_paid, 0) as 'Actual Payment Amount',
                                                 ifnull(paymentTbl.princ_paid, 0)as 'Principal Paid' ,
                                                 ifnull(paymentTbl.int_paid, 0) as 'Interest Paid',
                                                 l.fee_charges_repaid_derived as 'Insurance fee Paid',
                                                 ifnull(penaltiesTbl.Pen_outstanding, 0) as 'Total Late Fees',
                                                 ifnull(l.total_overpaid_derived, 0) as 'Excess Amount Paid',
                                                 ifnull(waiverTbl.int_waived, 0) as 'Interest Waived' ,
                                                case
                                                     when l.loan_status_id in (300, 600, 601, 602, 700) then( l.total_expected_repayment_derived -fee_charges_repaid_derived - ifnull(paymentTbl.princ_paid, 0)- ifnull(paymentTbl.int_paid, 0)-ifnull(waiverTbl.int_waived, 0)-ifnull(penaltiesTbl.pen_waived, 0)- ifnull(paymentTbl.pen_paid, 0))
                                                     else 0
                                                 end as 'Current Balance',
                                                 case
                                                     when l.loan_status_id in(300, 600, 601, 602, 700) then (l.principal_disbursed_derived- ifnull(paymentTbl.princ_paid, 0))
                                                     else 0
                                                 end as 'Principal Balance',
                                                 case
                                                     when l.loan_status_id in (300, 600, 601, 602, 700) then (l.interest_charged_derived - ifnull(paymentTbl.int_paid, 0)-ifnull(waiverTbl.int_waived, 0))
                                                     else 0
                                                 end as 'Interest Balance',
                                                 0 as 'Fees Balance',
                                                 (case
                                                     when (expected_princ - ifnull(paymentTbl.princ_paid, 0)) > 0 then (expected_princ - ifnull(paymentTbl.princ_paid, 0))
                                                     when (expected_princ - ifnull(paymentTbl.princ_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end) + (case
                                                     when (expected_int- ifnull(waiverTbl.int_waived, 0)- ifnull(paymentTbl.int_paid, 0)) > 0 then (expected_int - ifnull(waiverTbl.int_waived, 0) - ifnull(paymentTbl.int_paid, 0))
                                                     when (expected_int -ifnull(waiverTbl.int_waived, 0) - ifnull(paymentTbl.int_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end) + (case
                                                     when (expected_fees - ifnull(paymentTbl.fee_paid, 0)) > 0 then (expected_fees - ifnull(paymentTbl.fee_paid, 0))
                                                     when (expected_fees - ifnull(paymentTbl.fee_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end) as 'Amount Past Due',
                                                 case
                                                     when (expected_princ - ifnull(paymentTbl.princ_paid, 0)) > 0 then (expected_princ - ifnull(paymentTbl.princ_paid, 0))
                                                     when (expected_princ - ifnull(paymentTbl.princ_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end as 'Principal Past Due',
                                                 case
                                                     when (expected_int- ifnull(waiverTbl.int_waived, 0)- ifnull(paymentTbl.int_paid, 0)) > 0 then (expected_int - ifnull(waiverTbl.int_waived, 0) - ifnull(paymentTbl.int_paid, 0))
                                                     when (expected_int -ifnull(waiverTbl.int_waived, 0) - ifnull(paymentTbl.int_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end as 'Interest Past Due',
                                                 case
                                                     when (expected_fees - ifnull(paymentTbl.fee_paid, 0)) > 0 then (expected_fees - ifnull(paymentTbl.fee_paid, 0))
                                                     when (expected_fees - ifnull(paymentTbl.fee_paid, 0)) <= 0 then 0
                                                     else 0
                                                 end as 'Fees Past Due',
                                                 ifnull(nextPaymentTbl.scheduledPrincipalAmount, 0) as 'Scheduled Principal Amount',
                                                 ifnull(nextPaymentTbl.scheduledInterestAmount, 0) as 'Scheduled Interest Amount',
                                                 ifnull(nextPaymentTbl.scheduledFeesAmount, 0) as 'Scheduled Fees Amount',
                                                 ifnull(nextPaymentTbl.scheduledPaymentAmount, 0) as 'Scheduled Payment Amount',
                                                 ifnull(lastPaymentTbl.amount, 0) as 'Last Payment Amount',
                                                 ifnull(lastPaymentTbl.principal_portion_derived, 0) as 'Last Principal Amount',
                                                 ifnull(lastPaymentTbl.interest_portion_derived, 0) as 'Last Interest Amount',
                                                 ifnull(lastPaymentTbl.fee_charges_portion_derived, 0) as 'Last Fees Amount',
                                                 ifnull(lastPaymentTbl.fee_charges_portion_derived, 0) as 'Last Late Fees Amount',
                                                 ifnull(l.total_overpaid_derived, 0) as 'Last Excess Amount',
                                                 ifnull(case when max_date = min_date or max_date>min_date then datediff(date('${endDate}'), min_date) else datediff(max_date, min_date) End, 0) as 'Days in Arrears'
                                                 ,
                                                 ifnull(case when max_date >= curdate() then 0 else repaymentInstalmentTbl.Instalment end , 0 )as 'Installment in Arrears',
                                                 lastPaymentTbl.transaction_date as 'Last Payment Date',
                                                case
                                                     when (datediff(now(), laa.overdue_since_date_derived)>0
                                                     and l.maturedon_date<curdate()) then curdate()
                                                     else nextPaymentTbl.nextPaymentDueDate
                                                 end as 'Next Payment Due',
                                                 l.maturedon_date as 'Final Payment Date',
                                                 l.closedon_date as 'Date Closed',
                                                 case
                                                     when (con.enabled = false) then loanStatusTable.loanStatus
                                                     when con.enabled = true then case
                                                         when loanStatusTable.loanStatus = 'Pending Approval'
                                                         and loanStatusTable.loanDecisionState is null then loanStatusTable.loanStatus
                                                         when loanStatusTable.loanStatus = 'Pending Approval'
                                                         and loanStatusTable.loanDecisionState is not null
                                                         and loanStatusTable.loanDecisionState != 'Prepare And Sign Contract' then loanStatusTable.loanDecisionState
                                                         when loanStatusTable.loanStatus != 'Pending Approval' then loanStatusTable.loanStatus
                                                         when loanStatusTable.loanStatus = 'Pending Approval'
                                                         and loanStatusTable.loanDecisionState = 'Prepare And Sign Contract' then loanStatusTable.loanStatus
                                                     end
                                                 end as 'Loan Status' ,
                                                 l.description as 'Business Description' ,
                                                 case
 when c.legal_form_enum = 1 then bi.`Business Sector`
 when c.legal_form_enum = 2 then cvebis.code_value
 end as 'Industry/Sector of Activity',
 case
 when c.legal_form_enum = 1 then bi.`Business Sub-Sector`
 when c.legal_form_enum = 2 then cvebiss.code_value
 end as 'Business Sub-Sector',
                                                 l.created_on_utc,
                                                 l.last_modified_on_utc
                                             from
                                                 m_office o
                                             join m_office ounder ON
                                                 ounder.hierarchy like concat(o.hierarchy, '%')
                                                 AND ounder.hierarchy like CONCAT('${currentUserHierarchy}', '%')
                                             join m_client c ON
                                                 c.office_id = ounder.id
                                             join m_loan l ON
                                                 l.client_id = c.id
                                             left join m_loan_arrears_aging laa on
                                                 laa.loan_id = l.id
                                             join m_product_loan p ON
                                                 p.id = l.product_id
                                             join m_appuser u on
                                                 u.id = l.created_by
                                             left join m_staff st on
                                                 st.id = l.loan_officer_id
                                             left join m_currency currency ON
                                                 currency.code = p.currency_code
                                             left join m_code_value cvg ON
                                                 cvg.id = c.gender_cv_id
                                             left join m_client_other_info coi ON
                                                 coi.client_id = c.id
                                             left join m_client_additional_info cai on
                                                 cai.client_id = c.id
                                             left join m_client_recruitment_survey crs on
                                                 crs.client_id = c.id
                                             left join m_code_value cvn ON
                                                 cvn.id = coi.nationality_cv_id
                                             left join m_code_value cvs ON
                                                 cvs.id = coi.strata_cv_id
                                             left join m_code_value cvd ON
                                                 cvd.id = l.department_cv_id
                                             left join m_fund f ON
                                                 f.id = l.fund_id
                                             left join m_code_value cvc on
                                                 cvc.id = crs.cohort_cv_id
                                             left join m_business_detail bd on
                                                 bd.client_id = c.id
                                             left join m_code_value cvb on
                                                 cvb.id = bd.business_type_id
                                             left join m_loan_collateral_management lcm on
                                                 lcm.loan_id = l.id
                                             left join m_client_collateral_management_additional_details ccma on
                                                 ccma.client_collateral_id = lcm.client_collateral_id
                                             LEFT JOIN
                                                 (
                                                 SELECT
                                                     cdr.client_id,
                                                     MIN(addr.physical_address_sector) AS physical_address_sector,
                                                     MIN(addr.physical_address_district) AS physical_address_district,
                                                     MIN(addr.physical_address_cell) AS physical_address_cell,
                                                     MIN(addr.state_province_id) AS state_province_id
                                                 FROM
                                                     m_client_address cdr
                                                 JOIN
                                                      m_address addr ON
                                                     addr.id = cdr.address_id
                                                 GROUP BY
                                                     cdr.client_id
                                                  ) adr ON
                                                 c.id = adr.client_id
                                             left join m_client_non_person cid on
                                                 c.id = cid.client_id
                                             left join m_code_value cvp on
                                                 cvp.id = adr.state_province_id
                                             left join m_code_value loanPurposeTble on
                                                 loanPurposeTble.id = l.loanpurpose_cv_id
                                             left join (
                                                 select
                                                     *
                                                     from m_loan_transaction lt
                                                 where
                                                     lt.id in (
                                                     select
                                                         max(id)
                                                     from
                                                         m_loan_transaction t
                                                     where
                                                         t.transaction_type_enum = 2
                                                         and t.is_reversed = 0
                                                         and (date(t.transaction_date) between date('${startDate}') and date('${endDate}'))
                                                     group by
                                                         t.loan_id)) lastPaymentTbl on
                                                 lastPaymentTbl.loan_id = l.id
                                             left join (
                                                 select
                                                     *
                                                     from m_loan_repayment_schedule lt
                                                 where
                                                     lt.id in (
                                                     select
                                                         min(id)
                                                     from
                                                         m_loan_repayment_schedule rs
                                                     where
                                                         (date(rs.duedate) between date('${startDate}') and date('${endDate}'))
                                                     group by
                                                         rs.loan_id)) maxpaydateTbl on
                                                 maxpaydateTbl.loan_id = l.id
                                             left join (
                                                 select
                                                     t.loan_id,
                                                     sum(ifnull(t.principal_portion_derived, 0)) as princ_paid,
                                                     sum(ifnull(t.interest_portion_derived, 0)) as int_paid,
                                                     sum(ifnull(t.fee_charges_portion_derived, 0)) as fee_paid,
                                                     sum(ifnull(t.penalty_charges_portion_derived, 0)) as pen_paid
                                                 from
                                                     m_loan_transaction t
                                                 where
                                                     t.transaction_type_enum in (1, 2)
                                                         and t.is_reversed = 0
                                                         and (date(t.transaction_date) between date('${startDate}') and date('${endDate}'))
                                                     group by
                                                         t.loan_id) paymentTbl on
                                                 paymentTbl.loan_id = l.id
                                             left join (
                                                 select
                                                     t.loan_id,
                                                     sum(ifnull(t.amount, 0))as Actual_paid
                                                 from
                                                     m_loan_transaction t
                                                 where
                                                     t.transaction_type_enum in (2)
                                                         and t.is_reversed = 0
                                                         and (date(t.transaction_date) between date('${startDate}') and date('${endDate}'))
                                                     group by
                                                         t.loan_id) paymentTbl1 on
                                                 paymentTbl1.loan_id = l.id
                                             left join (
                                                 select
                                                     t.loan_id,
                                                     sum(ifnull(amount_outstanding_derived, 0))Pen_outstanding ,
                                                     sum(ifnull(amount_paid_derived, 0))Pen_paid,
                                                     sum(ifnull(amount_waived_derived, 0))pen_waived
                                                 from
                                                     m_loan_charge t
                                                 where
                                                     (date(t.due_for_collection_as_of_date) between date('${startDate}') and date('${endDate}'))
                                                 group by
                                                     t.loan_id) penaltiesTbl on
                                                 penaltiesTbl.loan_id = l.id
                                             left join (
                                                 select
                                                     t.loan_id,
                                                     sum(ifnull(t.interest_portion_derived, 0)) as int_waived,
                                                     sum(ifnull(t.fee_charges_portion_derived, 0)) as fee_waived,
                                                     sum(ifnull(t.penalty_charges_portion_derived, 0)) as pen_waived
                                                 from
                                                     m_loan_transaction t
                                                 where
                                                     t.transaction_type_enum = 4
                                                     and t.is_reversed = 0
                                                     and (date(t.transaction_date) between date('${startDate}') and date('${endDate}'))
                                                 group by
                                                     t.loan_id) waiverTbl on
                                                 waiverTbl.loan_id = l.id
                                             left join m_loan_decision ld on
                                                 ld.loan_id = l.id
                                             left join c_configuration con on
                                                 con.name = 'Add-More-Stages-To-A-Loan-Life-Cycle'
                                             left join (
                                                 select
                                                     l.id as loanId,
                                                     case
                                                         when l.loan_status_id = 100 then 'Pending Approval'
                                                         when l.loan_status_id = 200 then 'Approval'
                                                         when l.loan_status_id = 300 then 'Active'
                                                         when l.loan_status_id = 303 then 'Transfer In Progress'
                                                         when l.loan_status_id = 304 then 'Transfer On Hold'
                                                         when l.loan_status_id = 400 then 'Withdrawn By Client'
                                                         when l.loan_status_id = 500 then 'Rejected'
                                                         when l.loan_status_id = 600 then 'Closed Obligations Met'
                                                         when l.loan_status_id = 601 then 'Closed Written Off'
                                                         when l.loan_status_id = 602 then 'Closed Reschedule Outstanding Amount'
                                                         when l.loan_status_id = 700 then 'Overpaid'
                                                     end as loanStatus,
                                                     case
                                                         when l.loan_decision_state = 1000 then 'Review Application'
                                                         when l.loan_decision_state = 1200 then 'Due Diligence'
                                                         when l.loan_decision_state = 1300 then 'Collateral Review'
                                                         when l.loan_decision_state = 1400 then 'IC Review Level One'
                                                         when l.loan_decision_state = 1500 then 'IC Review Level Two'
                                                         when l.loan_decision_state = 1600 then 'IC Review Level Three'
                                                         when l.loan_decision_state = 1700 then 'IC Review Level Four'
                                                         when l.loan_decision_state = 1800 then 'IC Review Level Five'
                                                         when l.loan_decision_state = 1900 then 'Prepare And Sign Contract'
                                                     end as loanDecisionState
                                                 from
                                                     m_loan l
                                                 left join m_loan_decision ld on
                                                     ld.loan_id = l.id) as loanStatusTable on
                                                 loanStatusTable.loanId = l.id
                                             left join (
                                                 select
                                                     count(*) as installemntsCount,
                                                     lrs.loan_id
                                                 from
                                                     m_loan_repayment_schedule lrs
                                                 where
                                                     lrs.completed_derived = false
                                                     and lrs.duedate < now()
                                                 group by
                                                     lrs.loan_id,
                                                     lrs.id) installmentArrears on
                                                 installmentArrears.loan_id = l.id
                                             left join (
                                                 select
                                                     sum(lrs.principal_amount) as expected_princ,
                                                     sum(lrs.interest_amount) as expected_int,
                                                     ifnull(sum(lrs.fee_charges_amount), 0) as expected_fees,
                                                     lrs.loan_id
                                                 from
                                                     m_loan_repayment_schedule lrs
                                                 where
                                                     (date(lrs.duedate) between date('${startDate}') and date('${endDate}'))
                                                 group by
                                                     lrs.loan_id) expectedrepaymentTbl on
                                                 expectedrepaymentTbl.loan_id = l.id
                                             left join (
                                                 select
                                                     count(*) as Instalment,
                                                     lrs.loan_id,
                                                     max(lrs.duedate) as max_date,
                                                     min(lrs.duedate) as min_date
                                                 from
                                                     m_loan_repayment_schedule lrs
                                                 where
                                                     (date(lrs.duedate) between date('${startDate}') and date('${endDate}'))
                                                         and ((lrs.completed_derived = false
                                                             AND lrs.obligations_met_on_date IS NULL)
                                                         OR lrs.obligations_met_on_date > date('${endDate}'))
                                                     group by
                                                         lrs.loan_id) repaymentInstalmentTbl on
                                                 repaymentInstalmentTbl.loan_id = l.id
                                             LEFT JOIN (
                                                 SELECT
                                                     lrs.duedate AS nextPaymentDueDate,
                                                     lrs.loan_id,
                                                     IFNULL(lrs.principal_amount, 0) AS scheduledPrincipalAmount,
                                                     IFNULL(lrs.interest_amount, 0) AS scheduledInterestAmount,
                                                     IFNULL(lrs.fee_charges_amount, 0) AS scheduledFeesAmount,
                                                     IFNULL(lrs.principal_amount, 0) + IFNULL(lrs.interest_amount, 0) AS scheduledPaymentAmount
                                                 FROM
                                                     (
                                                     SELECT
                                                         lrs.*,
                                                         ROW_NUMBER() OVER (PARTITION BY lrs.loan_id
                                                     ORDER BY
                                                         lrs.installment ASC) AS row_num
                                                     FROM
                                                         m_loan_repayment_schedule lrs
                                                     WHERE
                                                         lrs.completed_derived = false
                                                         AND lrs.obligations_met_on_date IS NULL
                                                         and lrs.duedate >= curdate()
                                                     GROUP BY
                                                         lrs.loan_id,
                                                         lrs.installment,
                                                         lrs.id
                                                     ORDER BY
                                                         lrs.installment ASC ) lrs
                                                 WHERE
                                                     lrs.row_num = 1 ) AS nextPaymentTbl on
                                                 nextPaymentTbl.loan_id = l.id
                                             left join (
                                                 SELECT
                                                     account_no,
                                                     disbursedon_date,
                                                     created_on_utc,
                                                     submittedon_date,
                                                     client_id,
                                                     id as loan_id,
                                                     CAST(ROW_NUMBER() OVER (PARTITION BY client_id ORDER BY submittedon_date) AS SIGNED)AS incremental_count
                                                 FROM
                                                     m_loan)loancountTbl on
                                                 loancountTbl.client_id = l.client_id
                                                 and loancountTbl.loan_id = l.id
                                             left join `Business Information` bi on
                                                 c.id = bi.client_id
                                             left join `Entity Business Information` ebi on
c.id = ebi.client_id
 left join m_code_value cvebis on
ebi.`BusinessSectors_cd_Business Sector` = cvebis.id
 left join m_code_value cvebiss on
ebi.`BusinessSubSectors_cd_Business Sub-Sector` = cvebiss.id
 left join `Business Owner Information` boi on
c.id = boi.client_id
 left join m_code_value cvbog on
boi.`Gender_cd_Gender` = cvbog.id
 left join m_code_value cvbon on
boi.`COUNTRY_cd_Nationality` = cvbon.id
                                             where
                                                 o.id = ${officeId}
                                                 and (l.product_id = '${loanProductId}'
                                                     or '-1' = '${loanProductId}')
                                                 and (ifnull(l.loan_officer_id, -10) = '${loanOfficerId}'
                                                     or '-1' = '${loanOfficerId}')
                                                 and (ifnull(l.fund_id, -10) = ${fundId}
                                                     or -1 = ${fundId})
                                                 and (ifnull(l.loanpurpose_cv_id, -10) = ${loanPurposeId}
                                                     or -1 = ${loanPurposeId})
                                                 and (l.currency_code = '${currencyId}'
                                                     or '-1' = '${currencyId}')
                                                 and (date(l.disbursedon_date) between date('${startDate}') and date('${endDate}')) AND (${loanStatusId} = -1 OR l.loan_status_id = ${loanStatusId}) ]]></column>
            <where>report_name='Portfolio Management'</where>
        </update>
    </changeSet>
    <changeSet id="insert-loan-status-parameter" author="deepika@fiter.io">
        <insert tableName="stretchy_parameter">
            <column name="parameter_name" value="LoanAccountStatus"/>
            <column name="parameter_variable" value="loanStatusId"/>
            <column name="parameter_label" value="LoanAccountStatus"/>
            <column name="parameter_displayType" value="select"/>
            <column name="parameter_FormatType" value="number"/>
            <column name="parameter_default" value="-1"/>
            <column name="selectAll" value="Y"/>
            <column name="parameter_sql"><![CDATA[
                select * from (
                    select 300 as id, "Active" as name union all
                    select 303 as id, "Transfer In Progress" as name union all
                    select 304 as id, "Transfer On Hold" as name union all
                    select 400 as id, "Withdrawn By Client" as name union all
                    select 500 as id, "Rejected" as name union all
                    select 600 as id, "Closed Obligations Met" as name union all
                    select 601 as id, "Closed Written Off" as name union all
                    select 602 as id, "Closed Reschedule Outstanding Amount" as name union all
                    select 700 as id, "Overpaid" as name
                ) x order by x.id
            ]]>
            </column>
        </insert>
    </changeSet>
    <changeSet id="update-loanaccountstatus-to-portfolio-management" author="deepika@fiter.io">
        <insert tableName="stretchy_report_parameter">
            <column name="report_id"
                    valueComputed="(SELECT id FROM stretchy_report WHERE report_name = 'Portfolio Management')"/>
            <column name="parameter_id"
                    valueComputed="(SELECT id FROM stretchy_parameter WHERE parameter_name = 'LoanAccountStatus')"/>
            <column name="report_parameter_name" value="loanStatusId"/>
        </insert>
    </changeSet>

    <changeSet author="bosco@fiter.io" id="fix_portfolio_management_report_with_payoff_option">
        <update tableName="stretchy_report">
            <column name="report_sql"><![CDATA[

            SELECT
    DISTINCT l.id,
    l.account_no AS 'Loan Number',
    st.display_name AS 'Loan Officer',
    CASE
        WHEN c.legal_form_enum = 1 THEN CONCAT(c.firstname, ' ', c.lastname)
        WHEN c.legal_form_enum = 2 THEN c.display_name
    END AS 'Client Name',
    c.external_id AS 'Client UUID',
    CASE
        WHEN c.legal_form_enum = 1 THEN 'Individual'
        WHEN c.legal_form_enum = 2 THEN 'Entity'
    END AS 'Client Type',
    p.name AS 'Loan Product',
    loanPurposeTble.code_value AS 'Purpose',
    cvd.code_value AS 'Department',
    cvs.code_value AS 'Strata',
    l.kiva_id AS 'KIVA Loan ID',
    f.name AS 'Funder',
    CASE c.legal_form_enum
        WHEN 1 THEN COALESCE(coi.national_identification_number, 'NA')
        ELSE COALESCE(cid.incorp_no, 'NA')
    END AS 'Client ID',
    c.date_of_birth AS 'Date of Birth',
    c.kiva_id AS 'KIVA Client ID',
    loancountTbl.incremental_count AS 'Cycle',
    cvc.code_value AS 'Cohort',
    CASE
        WHEN c.legal_form_enum = 1 THEN cvg.code_value
        WHEN c.legal_form_enum = 2 THEN cvbog.code_value
    END AS 'Gender',
    cvp.code_value AS 'Province',
    adr.physical_address_sector AS 'Sector',
    adr.physical_address_cell AS 'Cell',
    adr.physical_address_district AS 'District',
    CASE
        WHEN c.legal_form_enum = 1 THEN cvn.code_value
        WHEN c.legal_form_enum = 2 THEN cvbon.code_value
    END AS 'Nationality',
    coi.telephone_no AS 'Telephone',
    cai.alt_phone_no AS 'Mobile No',
    l.submittedon_date AS 'Submission Date ',
    l.approvedon_date AS 'Approval Date',
    l.disbursedon_date AS 'Disbursement Date',
    l.principal_amount_proposed AS 'Applied Amount',
    l.approved_principal AS 'Approved Amount',
    l.principal_disbursed_derived AS 'Disbursed Amount',
    (l.approved_principal - l.principal_disbursed_derived) AS 'Difference',
    currency.name AS 'Currency Type',
    CASE l.repayment_period_frequency_enum
        WHEN 0 THEN 'Daily'
        WHEN 1 THEN 'Weelky'
        WHEN 2 THEN 'Monthly'
        WHEN 3 THEN 'Yearly'
    END AS 'Re-payment Term',
    CASE
        WHEN l.loan_type_enum = 1 THEN 'Individual'
        WHEN l.loan_type_enum = 2 THEN 'Group'
        WHEN l.loan_type_enum = 3 THEN 'JLG'
        WHEN l.loan_type_enum = 4 THEN 'GLIM'
        WHEN l.loan_type_enum = 5 THEN 'GSIM'
    END AS 'Loan Type',
    l.term_frequency AS 'Terms Duration',
    IFNULL(paymentTbl1.Actual_paid, 0) AS 'Actual Payment Amount',
    IFNULL(paymentTbl.princ_paid, 0) AS 'Principal Paid',
    IFNULL(paymentTbl.int_paid, 0) AS 'Interest Paid',
    l.fee_charges_repaid_derived AS 'Insurance fee Paid',
    IFNULL(penaltiesTbl.Pen_charged, 0) AS 'Total Late Fees',
    IFNULL(l.total_overpaid_derived, 0) AS 'Excess Amount Paid',
    IFNULL(waiverTblInterest.int_waived, 0) AS 'Interest Waived',
    IFNULL(waiverTblCharges.fee_waived, 0) AS 'Fee Waived',
    IFNULL(penaltiesTbl.pen_waived, 0) AS 'Penalty Waived',
    CASE
        WHEN l.loan_status_id IN (300, 600, 601, 602, 700)
            THEN ((l.principal_disbursed_derived + l.interest_charged_derived + l.fee_charges_charged_derived + ifnull(penaltiesTbl.Pen_charged, 0))
                - fee_charges_repaid_derived
                - IFNULL(waiverTblCharges.fee_waived, 0)
                - IFNULL(paymentTbl.princ_paid, 0)
                - IFNULL(paymentTbl.int_paid, 0)
                - IFNULL(waiverTblInterest.int_waived, 0)
                - IFNULL(penaltiesTbl.pen_waived, 0)
                - IFNULL(paymentTbl.pen_paid, 0)
                - IFNULL(writtenOffPortionTbl.principal_wo, 0)
            - IFNULL(writtenOffPortionTbl.interest_wo, 0)
            - IFNULL(writtenOffPortionTbl.fee_wo, 0)
            - IFNULL(writtenOffPortionTbl.penalty_wo, 0))
        ELSE 0
    END AS 'Current Balance',
    CASE
        WHEN l.loan_status_id IN (300, 600, 601, 602, 700)
            THEN (l.principal_disbursed_derived - IFNULL(paymentTbl.princ_paid, 0)  - IFNULL(writtenOffPortionTbl.principal_wo, 0))
        ELSE 0
    END AS 'Principal Balance',
    CASE
        WHEN l.loan_status_id IN (300, 600, 601, 602, 700)
            THEN (l.interest_charged_derived - IFNULL(paymentTbl.int_paid, 0) - IFNULL(waiverTblInterest.int_waived, 0) - IFNULL(writtenOffPortionTbl.interest_wo, 0))
        ELSE 0
    END AS 'Interest Balance',
    0 AS 'Fees Balance',
    -- FIXED Amount Past Due calculation - removed waiver deductions
    (
        CASE
            WHEN (expected_princ - IFNULL(paymentTbl.princ_paid, 0) - IFNULL(writtenOffPortionTbl.principal_wo, 0)) > 0
                THEN (expected_princ - IFNULL(paymentTbl.princ_paid, 0) - IFNULL(writtenOffPortionTbl.principal_wo, 0))
            ELSE 0
        END
        +
        CASE
            WHEN (expected_int - IFNULL(paymentTbl.int_paid, 0) - IFNULL(writtenOffPortionTbl.interest_wo, 0)) > 0
                THEN (expected_int - IFNULL(paymentTbl.int_paid, 0) - IFNULL(writtenOffPortionTbl.interest_wo, 0))
            ELSE 0
        END
        +
        CASE
            WHEN (expected_fees - IFNULL(paymentTbl.fee_paid, 0) - IFNULL(writtenOffPortionTbl.fee_wo, 0)) > 0
                THEN (expected_fees - IFNULL(paymentTbl.fee_paid, 0) - IFNULL(writtenOffPortionTbl.fee_wo, 0))
            ELSE 0
        END
        +
        CASE
            WHEN (expected_pen - IFNULL(paymentTbl.pen_paid, 0) - IFNULL(writtenOffPortionTbl.penalty_wo, 0)) > 0
                THEN (expected_pen - IFNULL(paymentTbl.pen_paid, 0) - IFNULL(writtenOffPortionTbl.penalty_wo, 0))
            ELSE 0
        END
    ) AS 'Amount Past Due',
    -- FIXED Principal Past Due calculation
    CASE
        WHEN (expected_princ - IFNULL(paymentTbl.princ_paid, 0) - IFNULL(writtenOffPortionTbl.principal_wo, 0)) > 0
            THEN (expected_princ - IFNULL(paymentTbl.princ_paid, 0) - IFNULL(writtenOffPortionTbl.principal_wo, 0))
        ELSE 0
    END AS 'Principal Past Due',
    -- FIXED Interest Past Due calculation
    CASE
        WHEN (expected_int - IFNULL(paymentTbl.int_paid, 0) - IFNULL(writtenOffPortionTbl.interest_wo, 0)) > 0
            THEN (expected_int - IFNULL(paymentTbl.int_paid, 0) - IFNULL(writtenOffPortionTbl.interest_wo, 0))
        ELSE 0
    END AS 'Interest Past Due',
    -- FIXED Fees Past Due calculation
    CASE
        WHEN (expected_fees - IFNULL(paymentTbl.fee_paid, 0) - IFNULL(writtenOffPortionTbl.fee_wo, 0)) > 0
            THEN (expected_fees - IFNULL(paymentTbl.fee_paid, 0) - IFNULL(writtenOffPortionTbl.fee_wo, 0))
        ELSE 0
    END AS 'Fees Past Due',
    -- FIXED Penalty Past Due calculation
    CASE
        WHEN (expected_pen - IFNULL(paymentTbl.pen_paid, 0) - IFNULL(writtenOffPortionTbl.penalty_wo, 0)) > 0
            THEN (expected_pen - IFNULL(paymentTbl.pen_paid, 0) - IFNULL(writtenOffPortionTbl.penalty_wo, 0))
        ELSE 0
    END AS 'Penalty Past Due',
    IFNULL(nextPaymentTbl.scheduledPrincipalAmount, 0) AS 'Scheduled Principal Amount',
    IFNULL(nextPaymentTbl.scheduledInterestAmount, 0) AS 'Scheduled Interest Amount',
    IFNULL(nextPaymentTbl.scheduledFeesAmount, 0) AS 'Scheduled Fees Amount',
    IFNULL(nextPaymentTbl.scheduledPaymentAmount, 0) AS 'Scheduled Payment Amount',
    IFNULL(lastPaymentTbl.amount, 0) AS 'Last Payment Amount',
    IFNULL(lastPaymentTbl.principal_portion_derived, 0) AS 'Last Principal Amount',
    IFNULL(lastPaymentTbl.interest_portion_derived, 0) AS 'Last Interest Amount',
    IFNULL(lastPaymentTbl.fee_charges_portion_derived, 0) AS 'Last Fees Amount',
    IFNULL(lastPaymentTbl.fee_charges_portion_derived, 0) AS 'Last Late Fees Amount',
    IFNULL(l.total_overpaid_derived, 0) AS 'Last Excess Amount',
    IFNULL(
        CASE
            WHEN max_date = min_date OR max_date > min_date THEN DATEDIFF(DATE('${endDate}'), min_date)
            ELSE DATEDIFF(max_date, min_date)
        END, 0
    ) AS 'Days in Arrears',
    IFNULL(
        CASE
            WHEN max_date >= CURDATE() THEN 0
            ELSE repaymentInstalmentTbl.Instalment
        END, 0
    ) AS 'Installment in Arrears',
    lastPaymentTbl.transaction_date AS 'Last Payment Date',
    CASE
        WHEN (DATEDIFF(NOW(), laa.overdue_since_date_derived) > 0 AND l.maturedon_date < CURDATE()) THEN CURDATE()
        ELSE nextPaymentTbl.nextPaymentDueDate
    END AS 'Next Payment Due',
    l.maturedon_date AS 'Final Payment Date',
    l.closedon_date AS 'Date Closed',
    CASE
        WHEN (con.enabled = FALSE) THEN loanStatusTable.loanStatus
        WHEN con.enabled = TRUE THEN
            CASE
                WHEN loanStatusTable.loanStatus = 'Pending Approval' AND loanStatusTable.loanDecisionState IS NULL THEN loanStatusTable.loanStatus
                WHEN loanStatusTable.loanStatus = 'Pending Approval' AND loanStatusTable.loanDecisionState IS NOT NULL AND loanStatusTable.loanDecisionState != 'Prepare And Sign Contract' THEN loanStatusTable.loanDecisionState
                WHEN loanStatusTable.loanStatus != 'Pending Approval' THEN loanStatusTable.loanStatus
                WHEN loanStatusTable.loanStatus = 'Pending Approval' AND loanStatusTable.loanDecisionState = 'Prepare And Sign Contract' THEN loanStatusTable.loanStatus
            END
    END AS 'Loan Status',
    l.description AS 'Business Description',
    CASE
        WHEN c.legal_form_enum = 1 THEN bi.`Business Sector`
        WHEN c.legal_form_enum = 2 THEN cvebis.code_value
    END AS 'Industry/Sector of Activity',
    CASE
        WHEN c.legal_form_enum = 1 THEN bi.`Business Sub-Sector`
        WHEN c.legal_form_enum = 2 THEN cvebiss.code_value
    END AS 'Business Sub-Sector',
    l.created_on_utc,
    l.last_modified_on_utc
FROM
    m_office o
    JOIN m_office ounder ON ounder.hierarchy LIKE CONCAT(o.hierarchy, '%')
        AND ounder.hierarchy LIKE CONCAT('${currentUserHierarchy}', '%')
    JOIN m_client c ON c.office_id = ounder.id
    JOIN m_loan l ON l.client_id = c.id
    LEFT JOIN m_loan_arrears_aging laa ON laa.loan_id = l.id
    JOIN m_product_loan p ON p.id = l.product_id
    JOIN m_appuser u ON u.id = l.created_by
    LEFT JOIN m_staff st ON st.id = l.loan_officer_id
    LEFT JOIN m_currency currency ON currency.code = p.currency_code
    LEFT JOIN m_code_value cvg ON cvg.id = c.gender_cv_id
    LEFT JOIN m_client_other_info coi ON coi.client_id = c.id
    LEFT JOIN m_client_additional_info cai ON cai.client_id = c.id
    LEFT JOIN m_client_recruitment_survey crs ON crs.client_id = c.id
    LEFT JOIN m_code_value cvn ON cvn.id = coi.nationality_cv_id
    LEFT JOIN m_code_value cvs ON cvs.id = coi.strata_cv_id
    LEFT JOIN m_code_value cvd ON cvd.id = l.department_cv_id
    LEFT JOIN m_fund f ON f.id = l.fund_id
    LEFT JOIN m_code_value cvc ON cvc.id = crs.cohort_cv_id
    LEFT JOIN m_business_detail bd ON bd.client_id = c.id
    LEFT JOIN m_code_value cvb ON cvb.id = bd.business_type_id
    LEFT JOIN m_loan_collateral_management lcm ON lcm.loan_id = l.id
    LEFT JOIN m_client_collateral_management_additional_details ccma ON ccma.client_collateral_id = lcm.client_collateral_id
    LEFT JOIN (
        SELECT
            cdr.client_id,
            MIN(addr.physical_address_sector) AS physical_address_sector,
            MIN(addr.physical_address_district) AS physical_address_district,
            MIN(addr.physical_address_cell) AS physical_address_cell,
            MIN(addr.state_province_id) AS state_province_id
        FROM
            m_client_address cdr
            JOIN m_address addr ON addr.id = cdr.address_id
        GROUP BY
            cdr.client_id
    ) adr ON c.id = adr.client_id
    LEFT JOIN m_client_non_person cid ON c.id = cid.client_id
    LEFT JOIN m_code_value cvp ON cvp.id = adr.state_province_id
    LEFT JOIN m_code_value loanPurposeTble ON loanPurposeTble.id = l.loanpurpose_cv_id
    LEFT JOIN (
        SELECT *
        FROM m_loan_transaction lt
        WHERE lt.id IN (
            SELECT MAX(id)
            FROM m_loan_transaction t
            WHERE t.transaction_type_enum IN (2,28)
                AND t.is_reversed = 0
                AND (DATE(t.transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
            GROUP BY t.loan_id
        )
    ) lastPaymentTbl ON lastPaymentTbl.loan_id = l.id
    LEFT JOIN (
        SELECT *
        FROM m_loan_repayment_schedule lt
        WHERE lt.id IN (
            SELECT MIN(id)
            FROM m_loan_repayment_schedule rs
            WHERE (DATE(rs.duedate) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
            GROUP BY rs.loan_id
        )
    ) maxpaydateTbl ON maxpaydateTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            t.loan_id,
            SUM(IFNULL(t.principal_portion_derived, 0)) AS princ_paid,
            SUM(IFNULL(t.interest_portion_derived, 0)) AS int_paid,
            SUM(IFNULL(t.fee_charges_portion_derived, 0)) AS fee_paid,
            SUM(IFNULL(t.penalty_charges_portion_derived, 0)) AS pen_paid
        FROM m_loan_transaction t
        WHERE t.transaction_type_enum IN (1, 2,28)
            AND t.is_reversed = 0
            AND (DATE(t.transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
        GROUP BY t.loan_id
    ) paymentTbl ON paymentTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            t.loan_id,
            SUM(IFNULL(t.amount, 0)) AS Actual_paid
        FROM m_loan_transaction t
        WHERE t.transaction_type_enum IN (2,28)
            AND t.is_reversed = 0
            AND (DATE(t.transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
        GROUP BY t.loan_id
    ) paymentTbl1 ON paymentTbl1.loan_id = l.id
    LEFT JOIN (
        SELECT
            t.loan_id,
            SUM(IFNULL(amount_outstanding_derived, 0)) AS Pen_outstanding,
            SUM(IFNULL(amount_paid_derived, 0)) AS Pen_paid,
            SUM(IFNULL(amount_waived_derived, 0)) AS pen_waived,
            SUM(IFNULL(amount, 0)) AS Pen_charged
        FROM m_loan_charge t
        WHERE (DATE(t.due_for_collection_as_of_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
        GROUP BY t.loan_id
    ) penaltiesTbl ON penaltiesTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            t.loan_id,
            SUM(IFNULL(t.amount, 0)) AS int_waived
        FROM m_loan_transaction t
        WHERE t.transaction_type_enum = 4
            AND t.is_reversed = 0
            AND (DATE(t.transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
        GROUP BY t.loan_id
    ) waiverTblInterest ON waiverTblInterest.loan_id = l.id
    LEFT JOIN (
        SELECT
            t.loan_id,
            SUM(IFNULL(t.fee_charges_portion_derived, 0)) AS fee_waived
        FROM m_loan_transaction t
        WHERE t.transaction_type_enum = 9
            AND t.is_reversed = 0
            AND (DATE(t.transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
        GROUP BY t.loan_id
    ) waiverTblCharges ON waiverTblCharges.loan_id = l.id
    LEFT JOIN (
    SELECT
        loan_id,
        SUM(IFNULL(principal_portion_derived, 0)) AS principal_wo,
        SUM(IFNULL(interest_portion_derived, 0)) AS interest_wo,
        SUM(IFNULL(fee_charges_portion_derived, 0)) AS fee_wo,
        SUM(IFNULL(penalty_charges_portion_derived, 0)) AS penalty_wo
    FROM m_loan_transaction
    WHERE transaction_type_enum = 6
        AND is_reversed = 0
        AND (DATE(transaction_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
    GROUP BY loan_id
   ) writtenOffPortionTbl ON writtenOffPortionTbl.loan_id = l.id
    LEFT JOIN m_loan_decision ld ON ld.loan_id = l.id
    LEFT JOIN c_configuration con ON con.name = 'Add-More-Stages-To-A-Loan-Life-Cycle'
    LEFT JOIN (
        SELECT
            l.id AS loanId,
            CASE
                WHEN l.loan_status_id = 100 THEN 'Pending Approval'
                WHEN l.loan_status_id = 200 THEN 'Approval'
                WHEN l.loan_status_id = 300 THEN 'Active'
                WHEN l.loan_status_id = 303 THEN 'Transfer In Progress'
                WHEN l.loan_status_id = 304 THEN 'Transfer On Hold'
                WHEN l.loan_status_id = 400 THEN 'Withdrawn By Client'
                WHEN l.loan_status_id = 500 THEN 'Rejected'
                WHEN l.loan_status_id = 600 THEN 'Closed Obligations Met'
                WHEN l.loan_status_id = 601 THEN 'Closed Written Off'
                WHEN l.loan_status_id = 602 THEN 'Closed Reschedule Outstanding Amount'
                WHEN l.loan_status_id = 700 THEN 'Overpaid'
            END AS loanStatus,
            CASE
                WHEN l.loan_decision_state = 1000 THEN 'Review Application'
                WHEN l.loan_decision_state = 1200 THEN 'Due Diligence'
                WHEN l.loan_decision_state = 1300 THEN 'Collateral Review'
                WHEN l.loan_decision_state = 1400 THEN 'IC Review Level One'
                WHEN l.loan_decision_state = 1500 THEN 'IC Review Level Two'
                WHEN l.loan_decision_state = 1600 THEN 'IC Review Level Three'
                WHEN l.loan_decision_state = 1700 THEN 'IC Review Level Four'
                WHEN l.loan_decision_state = 1800 THEN 'IC Review Level Five'
                WHEN l.loan_decision_state = 1900 THEN 'Prepare And Sign Contract'
            END AS loanDecisionState
        FROM m_loan l
        LEFT JOIN m_loan_decision ld ON ld.loan_id = l.id
    ) AS loanStatusTable ON loanStatusTable.loanId = l.id
    LEFT JOIN (
        SELECT
            COUNT(*) AS installemntsCount,
            lrs.loan_id
        FROM m_loan_repayment_schedule lrs
        WHERE lrs.completed_derived = FALSE
            AND lrs.duedate < NOW()
        GROUP BY lrs.loan_id, lrs.id
    ) installmentArrears ON installmentArrears.loan_id = l.id
    LEFT JOIN (
        SELECT
            SUM(lrs.principal_amount) AS expected_princ,
            SUM(lrs.interest_amount) AS expected_int,
            IFNULL(SUM(lrs.fee_charges_amount), 0) AS expected_fees,
            IFNULL(SUM(lrs.penalty_charges_amount), 0) AS expected_pen,
            lrs.loan_id
        FROM m_loan_repayment_schedule lrs
        WHERE (DATE(lrs.duedate) BETWEEN DATE('${startDate}') AND DATE(DATE_SUB('${endDate}', INTERVAL 1 DAY)))
        GROUP BY lrs.loan_id
    ) expectedrepaymentTbl ON expectedrepaymentTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            COUNT(*) AS Instalment,
            lrs.loan_id,
            MAX(lrs.duedate) AS max_date,
            MIN(lrs.duedate) AS min_date
        FROM m_loan_repayment_schedule lrs
        WHERE (DATE(lrs.duedate) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
            AND (
                (lrs.completed_derived = FALSE AND lrs.obligations_met_on_date IS NULL)
                OR lrs.obligations_met_on_date > DATE('${endDate}')
            )
        GROUP BY lrs.loan_id
    ) repaymentInstalmentTbl ON repaymentInstalmentTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            lrs.duedate AS nextPaymentDueDate,
            lrs.loan_id,
            IFNULL(lrs.principal_amount, 0) AS scheduledPrincipalAmount,
            IFNULL(lrs.interest_amount, 0) AS scheduledInterestAmount,
            IFNULL(lrs.fee_charges_amount, 0) AS scheduledFeesAmount,
            IFNULL(lrs.principal_amount, 0) + IFNULL(lrs.interest_amount, 0) AS scheduledPaymentAmount
        FROM (
            SELECT
                lrs.*,
                ROW_NUMBER() OVER (PARTITION BY lrs.loan_id ORDER BY lrs.installment ASC) AS row_num
            FROM m_loan_repayment_schedule lrs
            WHERE lrs.completed_derived = FALSE
                AND lrs.obligations_met_on_date IS NULL
                AND lrs.duedate >= CURDATE()
            GROUP BY lrs.loan_id, lrs.installment, lrs.id
            ORDER BY lrs.installment ASC
        ) lrs
        WHERE lrs.row_num = 1
    ) AS nextPaymentTbl ON nextPaymentTbl.loan_id = l.id
    LEFT JOIN (
        SELECT
            account_no,
            disbursedon_date,
            created_on_utc,
            submittedon_date,
            client_id,
            id AS loan_id,
            CAST(ROW_NUMBER() OVER (PARTITION BY client_id ORDER BY submittedon_date) AS SIGNED) AS incremental_count
        FROM m_loan
    ) loancountTbl ON loancountTbl.client_id = l.client_id AND loancountTbl.loan_id = l.id
    LEFT JOIN `Business Information` bi ON c.id = bi.client_id
    LEFT JOIN `Entity Business Information` ebi ON c.id = ebi.client_id
    LEFT JOIN m_code_value cvebis ON ebi.`BusinessSectors_cd_Business Sector` = cvebis.id
    LEFT JOIN m_code_value cvebiss ON ebi.`BusinessSubSectors_cd_Business Sub-Sector` = cvebiss.id
    LEFT JOIN `Business Owner Information` boi ON c.id = boi.client_id
    LEFT JOIN m_code_value cvbog ON boi.`Gender_cd_Gender` = cvbog.id
    LEFT JOIN m_code_value cvbon ON boi.`COUNTRY_cd_Nationality` = cvbon.id
WHERE
    o.id = ${officeId}
    AND (l.product_id = '${loanProductId}' OR '-1' = '${loanProductId}')
    AND (IFNULL(l.loan_officer_id, -10) = '${loanOfficerId}' OR '-1' = '${loanOfficerId}')
    AND (IFNULL(l.fund_id, -10) = ${fundId} OR -1 = ${fundId})
    AND (IFNULL(l.loanpurpose_cv_id, -10) = ${loanPurposeId} OR -1 = ${loanPurposeId})
    AND (l.currency_code = '${currencyId}' OR '-1' = '${currencyId}')
    AND (DATE(l.disbursedon_date) BETWEEN DATE('${startDate}') AND DATE('${endDate}'))
    AND (${loanStatusId} = -1 OR l.loan_status_id = ${loanStatusId})

            ]]></column>
            <where>report_name='Portfolio Management'</where>
        </update>
    </changeSet>

</databaseChangeLog>
